apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-stable
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
  type: ClusterIP
